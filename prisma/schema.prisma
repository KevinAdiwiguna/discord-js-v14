generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Guild {
  guild_id   String      @id @unique
  guild_name String
  Minecraft  Minecraft[]
  Reminder   Reminder[]
  Welcome    Welcome?
}

model Minecraft {
  guild_id           String
  server_name        String
  server_address     String
  server_description String?
  server_version     String?
  id                 String  @id @default(uuid())
  guild              Guild   @relation(fields: [guild_id], references: [guild_id])
}

model Reminder {
  reminder_id String  @id @default(uuid())
  guild_id    String
  message_id  String
  time_id     String
  guild       Guild   @relation(fields: [guild_id], references: [guild_id])
  message     Message @relation(fields: [message_id], references: [message_id])
  timeid      Time    @relation(fields: [time_id], references: [time_id])
}

model Time {
  time_id  String     @id @default(uuid())
  hour     String
  minute   String
  day      String
  month    String
  year     String
  Reminder Reminder[]
}

model Welcome {
  guild_id   String  @id
  channel_id String?
  message_id String
  guild      Guild   @relation(fields: [guild_id], references: [guild_id])
  message    Message @relation(fields: [message_id], references: [message_id])
}

model Message {
  message_id String      @id @default(uuid())
  content    String
  type       MessageType
  images_url String?
  Reminder   Reminder[]
  Welcome    Welcome[]
}

enum MessageType {
  WELCOME
  REMINDER
}
